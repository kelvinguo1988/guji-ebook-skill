---
name: guji-ebook
description: 古籍电子书制作规范，支持两种成品风格（A 整理本 / B 复刻本）。当任务涉及"把扫描古籍 PDF 整理成书/电子书、多版本对读比对、注释白话翻译、书影配图、简繁混排、生成印刷级 PDF 与 EPUB"时用品类 A；当任务涉及"古籍复刻、直排竖排、刻本风格、木刻原貌、仿古书叶、做旧宣纸、像 vRain 那样的复刻电子书"时用品类 B；两种都要或未指明时按 §0.1 任务分诊。沉淀自道德经/穷通宝鉴/黄帝内经素问三部全书实战（字体回退、图版张冠李戴、mm/pt 单位、增补区异体字等踩坑教训）。
---

# 古籍整理电子书制作规范

从扫描古籍到成品书的完整规范。参考实现：`道德经样张/`（book.html 设计系统 + build_pdf.sh + build_epub_sample.py，12 页第一章样张已经 5 轮 judge 验收）。

## 0.1 任务分诊：先问清品类，再进管线

| 用户意图（关键词） | 品类 | 管线入口 |
|---|---|---|
| 整理、对读、注释、今译/白话、校勘、书影配图、"做本电子书"（未指明风格） | **A 整理本** | `build_*_full.py` → HTML → render_audit.sh → 盖页码 → EPUB/FXL |
| 复刻、直排/竖排、刻本风格、仿古原貌、木刻风、宣纸书叶、vRain 式 | **B 复刻本** | `build_*_fuke.py` → 直排对页 PDF |
| "两种都要"/对比着看 | 先 A 后 B（共享同一 book_data.json，成本只多一次渲染） | 两条管线串行 |

分诊后向用户复述一句所选品类与理由；样章先行（A 出第一章 PDF+EPUB，B 出一页对页样本），确认后再扩全书。

**分诊第二问：有没有原书扫描 PDF？**（用户定稿的版式分支）——有原书扫描的书，成品采用**一半原书、一半复刻/整理文**的对读结构（见 §0.5 第 17 条）；只有文字录文（无扫描）则**不做原书对照部分**，切勿用排版截图冒充"书影"。

## 0. 总管线（单一来源）

```
扫描 PDF 群 ──定位/裁切/核验──▶ 书影 assets/
整理数据(释文/古注) ──────────▶ book_data.json
                │
book.html（版式设计系统 + 全部内容，唯一手工源）
                ├──▶ build_pdf.sh  ──▶ 印刷 PDF（含字体审计自动重渲）
                └──▶ build_epub_sample.py ──▶ EPUB 3 ──▶ epubcheck
```

- **改书只改 book.html 一处**。EPUB 内容由脚本从 book.html 正则提取（`<section class="page">` 切片），不要在 EPUB 脚本里手工维护第二份文案（已踩坑：文案漂移）。
- 渲染：`Edge headless --print-to-pdf --virtual-time-budget=30000`（无需 Playwright；Edge 即 Chromium）。

## 0.5 输出品类：整理本与复刻本（一书两线）

同一 `book_data.json`（经/注/校正三层 + 阐释层）驱动两条输出线，**单一来源原则不变**：

- **品类 A 整理本**（本文件主体管线）：横排流式版式、简繁分工、随文阐释（章旨/白话/术语）、书影配图——面向"读懂"。
- **品类 B 复刻本**（数字复刻，参考 [vRain](https://github.com/shanleiguang/vRain) 思路、Python 自研 `build_*_fuke.py`）：直排右起、按原书行款逐字落格，刻本风格电子书——面向"原貌沉浸"。实现要点：
  1. **行款参数化**：版框尺寸/列数/每行字数/黑口白口/鱼尾/边栏全部参数取自版本著录或叶面实测（如元刻素问：半叶十三行、行二十三字、小字双行、黑口、四周双边、框高205×宽124mm）。一书一参数组。
  1b. **对齐 vRain 视觉的正确姿势——抄它的 cfg 而不是猜**：仓库 `canvas/24_*.cfg`（画布 2480×1860px@240dpi、书口 120px、鱼尾=五边形 rect 50+tri 30px 于 y=450/1550、外框 10px 内线 1px、margins 200/50/50/50）+ `books/NN/book.cfg`（row_num、text/comment font size、title/pager 位置、标点归一化规则、fallback_bold）+ `canvas/canvas.pl`（鱼尾五边形路径：书口宽顶线、两侧垂 rect+tri、中央 V 口）。**单位陷阱**：px→pt 乘 72/240=0.3，勿把 mm 数值当 pt 用（差 2.83 倍，字会小一半）。**密排**是刻本观感关键：墨迹(≈0.75em)≈行距，字号略大于行距。背景直接用仓库 `canvas/paper.jpg` 宣纸扫描图，勿自制噪声纹理。排版列数与渲染列数必须同源（旧参数覆盖新参数是隐形 bug 源）。
  2. **注双行平衡式**：注段整段缓冲后排版——右小列 ceil(n/2) 字、左小列 floor(n/2) 字，占 ceil(n/2) 个大字行；当前列放不下则整列起排；**注毕经文同列续排**（切勿注毕强制换列，会产生半空断叶）。流式逐字分配左右小列是错误的（读序需右列全部→左列全部）。
  3. **句读朱圈**：用带标点的整理录文（如殆知阁）与复刻正文流做 difflib 全局对齐（容异体/异文），句读处画朱圈——还原原书句读而无需人工标点。
  4. **版刻特征还原**：墨钉（实察记录的未刻字位画墨块）、篇题墨圈○、朱圈叶次等逐项入参数表。
  5. **字体（已鉴定 vRain 正主）**：vRain 主字体 = **齊伋體 qiji-combo.ttf**（font1，与其 repo fonts/ 内文件同源——经 MD5 比对与 [LingDong-/qiji-font](https://github.com/LingDong-/qiji-font) release 0.0.4 完全一致；font2/3=HanaMinA/B 花園明朝补 CJK 扩展区）。齊伋體是令東（Lingdong Huang）从明代凌閔刻本扫描页以半自动 CV+OCR 逐字抠取重建的明体（致敬明代出版家閔齊伋；作者声明字体正名为「令東齊伋體」），combo 版 = 主字形 4569 字 + 生僻字 fallback 合并 19665 字形（FontForge 合成，name 表被洗为 QIJIC v0.0.1）。复刻本直接用 `fonts/qiji-combo.ttf` 即与 vRain 同字体效果；`fitz.Font(fontfile=…).text_length` 测宽（`get_text_length` 不认自定义字体）。
  6. vRain 本体（Perl）依赖 PDF::Builder/Image::Magick 全套 CPAN 模块，本机未装则自研；其"文本标记系统"（【】双行注、$换半叶等）的**参数化思想**是复刻引擎设计参照。
  7. **对齐 vRain Example 的对页版式**（judge 对照验收 9.5/10）：一页 = 左右两半叶对页 + 中央书口（上下黑大鱼尾 + 黑底白字书名章 + 汉字页码）；宣纸做旧纹理背景（numpy 噪声+纤维线+边缘晕影，一次生成复用）；粗外框(2.6pt)+细内框；正文活字感 = fill+stroke 描边加粗（`render_mode=2, border_width≈0.075`，过重会糊成墨团）；注中引用书名朱红字（书名词典逐词染色）；框外右上书眉竖排书名 + 藏书印（vYinn 式电子印章，用户定稿：移植 [shanleiguang/vYinn](https://github.com/shanleiguang/vYinn) 分层流程——黑底白=印泥、阳文圆角框环、篆书字体逐字排版（拉伸/旋转/描边）、Spread 边扩+破残斑点 Multiply+OTSU+模糊油墨 → #874434 透明底 PNG 贴入，见 `make_seal.py`；篆书字体 fonts/chongxi_seal.otf 取自该 repo）。
  8. **越界检查必做**：注双行排版在 `row+tr ≥ ROWS` 时必须先换列再放字，且 put_big 应在放置前检查（放后检查会让满列后的首字落在框外 row=ROWS）；程序遍历 cells 断言 `row<ROWS and col<COLS`。末叶孤字属刻本自然收尾，judge 按豁免评判。
  9. **010.png 实测几何（judge 两轮 7.6→8.6 可交付）**：整个对页用**单一粗外框(10px)+细内框(1px)双线**围合（不是每半叶各一圈），书口 120px 双细线居中；鱼尾=贴版框上下沿的实心垂带五边形（rect50+tri30，上鱼尾 y450 尖朝下、下鱼尾 y1800 尖朝上）；书口题名 65–70px 自上鱼尾下起排、叶次 30–34px 居中偏下；天头留 ~440px 放书眉竖排书名+框外右上藏书印。正文列线在框内通高。
  10. **大字字号硬约束**：单列大字字号 ≤ 0.94×列宽（列宽 80.5px 时 84px 即压框、76px 安全）；大题/篇题用分数行距（如 16 字均布 23 行 = 步长 1.4375 行），ASSERT 分数行不越 ROWS。
  11. **句读圈密度**：只用录文"。"密度不够（60/835≈7%，judge 判"全页无圈"）；旧刻读点"，、"同样出圈，总密度 ≈15–20%（每列 3–6 处）才对齐样板观感。圈位=字右下（圆心 x+0.30em, y+0.32em，半径 0.13em，色 #874434）。
  12. **背景图必须 xref 复用**：每页 `insert_image(filename=…)` 会把 3 页 PDF 撑到 24MB；首页嵌入取 xref、后续页 `insert_image(xref=…)` 复用 + `doc.subset_fonts()` → 1.7MB。vRain 宣纸贴图直接铺偏白，叠一层 10% 不透明度茶色矩形 `(0.72,0.60,0.42, fill_opacity=0.10)` 即到样板做旧档。
  13. **行款与卷端结构以底本扫描实测为准，judge 的"惯例"意见要让位于叶面证据**：紫微斗数曾误记行 20 字（实测 23）；牌记/题署列在本书卷端实测自第 10 行起排（下半叶），judge 按刻本惯例建议列顶起排——经比对原叶扫描驳回。qiji-combo 缺 ○/〇 字形，圈类符号一律矢量绘制。
  14. **色彩三层体系（对齐 vRain《虞初新志》books/01 样板实测）**：正文=黑；句读圈=**朱砂毛笔圈（用户定稿）**——勿用等宽矢量正圆，用变宽闭合环（外缘正序+内缘逆序 fill 自动闭合、内孔偏心+正弦起伏+起笔顿厚收笔回叠，种子=字符位置保证渲染可复现；注意旧版 PyMuPDF draw_polyline 无 close_path 参数），末行圈 clamp 至版框下缘内 ≥9px；注/批=朱 #874434（45/60≈0.75 比例仅适用于 vRain 宽列，密排行款下注字必须 ≤0.94×半列宽，如 10 行款半列 40.25px→注 36px）；书名号《》→字左朱侧线（逐字 ±0.5 行距连续段）。标签层（朱底白字圆角块/朱圈白字，如「紅依」「詩」「傳」）为可选装饰，底本无则不造。
  15. **朱注整列让位式**：注触发时正文 flush 到次列，注独占一列（右小列先满 23 字→左小列→溢出另起注列），注列中缝画朱细线；切勿把注塞进正文列的半列缝里（列线穿注字、行气打架）。数据接口 = book_data.json 可选 `notes: [[锚点末几字, 注文], …]`，锚点 assert 必命中。
  16. **卷首封面叶嵌真叶书影**：右半嵌卷端扫描裁框图（ImageOps.colorize 暖灰做旧至 (96,78,62)–(238,235,226) 再与原图 blend 0.40 保留藏印朱色），左半白叶、书口不盖叶次——对齐 vRain 天头嵌 yin 图做法且纸色贴画布不"贴片"。末行句读圈 clamp：cy ≤ 版框下缘−r−9px，防圈叠内框线。
  17. **一半原书一半复刻的对读结构（用户定稿，按输入分支）**：凡持有原书扫描 PDF，成品即按"原书页 ↔ 复刻/整理页"一一对应对读排布——A 整理本用固定对页「每页两帧并排」（左帧原叶扫描按原叶比例整叶完整显示、右帧对应整理/复刻文，尺寸预算固定，勿流式分页截断叶图）；**B 复刻本逐叶对应：每对页左半=对应原书叶截图、右半=该叶复刻文**（`render_pair_page`），无扫描的叶留白叶（画界栏不排字），叶图整叶完整+题注（题注字符须过字体白名单验证）；切勿把原叶对照集中堆在卷首几页而与复刻正文脱钩（已踩坑：用户判为排版错误）。只有文字录文（殆知阁/维基文库等）而无扫描时，此分支整体关闭：不排原书帧、不做"书影"占位。图版内容必须能对文字负责（§3 可核实原则，叶位以图面实察为准）。

## 1. 字体规范 ★最高优先级（本项目最大坑）

### 1.1 字族选择
- **正文首选「完整版 Noto Serif CJK TC 全字库 OTF 单面方案」**（终版方案，本项目 421 页全书验证）：
  - 直接下载 notofonts/noto-cjk release 的完整 OTF（Regular+Bold 各约 25MB，raw.githubusercontent 可断点续传），以无 `unicode-range` 的 `@font-face` 挂载：全字库同时覆盖繁体、简体专用码位、扩展 A 区生僻字（儽飂讁繟䘒等王弼注文用字），单字面零混排。
  - 切片子集方案（Google Fonts css2 的 woff2）有两个坑：① TC 切片不含简体专用码位（写/与/书/馆/核/对/释/义…），简繁混排必回退；② 切片不含扩展 A 区，古注生僻字回退。若确需子集（体积敏感），必须「子集在前 + 完整 OTF 无 range 兜底在后」双层声明，且同族内缺字形不会自动下沉到后一个 face——必须以出片审计兜底。
  - 切勿用日文字库打底的字体（如霞鹜文楷）做繁体正文。
  - 字重：Regular 与 Bold 两个 OTF 分别声明 400 与 700，另为 500/600、800/900 补充声明（Chromium 对 range 描述符支持不稳，离散声明更可靠）。
- **楷体/辅助字体白名单制**：只允许用在逐字验证过字符集的字符串上（如封面「道德經」、章题「道可道，非常道」）。新字符串要用楷体，先跑 §1.3 测试。
- 字重层级靠同一家族的 400/600/700 区分，禁止引入第二衬线家族。

### 1.2 字体本地化
- 完整 OTF 放 `fonts/`，css 用相对路径引用即可（file:// 加载可靠）。
- 若用切片子集：从 css2 API 下载全部 woff2 并 base64 内联（`url(data:font/woff2;base64,…)`）——独立小文件加载存在竞态，virtual-time-budget 的虚拟时间可能在部分子集加载完成前到期，导致随机回退（同一文件两次渲染结果不同的根源）。
- 校验文件完整性：OTF magic = `OTTO`、sfnt 表数合理；woff2 magic = `wOF2` 且声明长度 == 实际长度。大文件下载易截断，必须校验。

### 1.3 零回退验证（两个必跑测试）
1. **无回退判别测试**：`font-family:"Noto Serif TC",monospace`（末级故意用等宽体），渲染待检字符串（含简体专用字、繁体字、扩展区生僻字三类）。字形是衬线 → 有字形；被打成等宽 → 缺字。
2. **出片审计**：PyMuPDF 遍历每页 `get_fonts()`，出现 `STSongti|PingFang|Hiragino|SongtiSC` 等系统字体名 = 有回退 = 构建失败。已封装在 `build_pdf.sh`（审计失败自动重渲，最多 4 次；**确定性失败 ≠ 竞态，必是字族覆盖问题，回到 §1.1**——本项目即靠"每次渲染失败集合完全相同"识别出覆盖缺陷而非竞态）。

## 2. 内容体例

- **简繁分工（铁律，凡例中声明并全书贯彻）**：经文/古注引文/帛书释文/异文/书名引用 = 繁体；凡例、导读、释义、今译、校按、跋页等现代阐释 = 简体。校勘表内引用列全繁体、"备考/校按"列简体。同一句内繁简并见即为 bug。
- **帛书释文标注**：〔　〕残缺拟补（标红），（　）通假今字（小号红字上标）。图例必须随页给出。
- **对读结构**：每章 = 章首（导读+章旨）→ 四版本对读（竖排面板）→ 书影 → 笺注（简体释义+繁体古注引用）→ 今译 → 校勘记（异文表+校按）。
- **数据源**：整理本释文/古注先入 `book_data.json`，再入稿；引文与藏本叶面逐字核对（扫描件就是最终底本）。
- **无文本层扫描的录文获取路线**：① 维基文库 API（`action=parse&prop=text`，`<pages>` 跨异自动展开）——四部叢刊等影印本多有全文录文且**含注**（注以〈〉包夹、「新校正云」随文）；注意剥除 `<style>` 泄漏与篇题行尾直接跟〈注〉的情况（匹配篇题先按〈截断）。② 殆知阁文献库 GitHub dump（`daizhigev20` 等）——简体、无注、**段落划分干净**，适合作段落锚与双源交叉质检。③ ctext.org 被 Cloudflare 拦，curl 不可用。录文体例可声明「经注以通行本系统录文、与藏本叶面通校/抽核」，叶面证据（题署/篇题/句读/墨钉）单独记入版刻对读。
- **数据清洗三件套（生成器内置，缺一不可）**：
  1. **OpenCC s2t 转繁**：整理数据里王弼注/河上公注多为简体录入（第一章手工核对过繁体，第 2 章起全是简体），古注渲染前必须转繁；
  2. **HTML 实体解码**：数据含 `&ldquo;&rdquo;&ircquo;` 等残留，先解码后剔未知实体；
  3. **句对污染过滤**：河上公数据的 `pairs` 混有卷末题记（「卷之四竟」）与编号勘误条目（「9.據影宋本加」），过滤规则 = 经文段含数字/超30字/含影宋勘誤卷之竟 → 剔；注文剥离脚注编号后仍含数字 → 剔。被污染章的经文从洁净句对重建（拼接即可，句首自带标点）。

## 3. 书影配图规范

- **可核实原则（最重要）**：配图内容必须能对文字负责。无法确证图版对应章节时，只标注可确证的信息（如「卷端书影」），绝不硬标"第 X 章"。已有教训：旧版书把今本 52–56 章图版标成了第一章。
- 定位方法：先查页边栏名（确定文献归属）→ 行号 → 释文行号对照。**凭隶书字形猜章节不可靠**，判读两次结果冲突即视为不可核实。
- 构图：以文字内容为主体，印章只是时代痕迹（少量）；书名列尽量完整保留。
- 规格：印刷 300dpi；图框内 object-fit:contain；图题「图X + 题名」+ 来源行（版本/收藏机构）；**成对图版必须固定图框高度**（`.ph{flex:0 0 auto;height:Xmm}`），否则两图比例不同会导致题注基线错位。
- 帛书类深暗残泐图：如实说明「丝帛深暗、残泐」，不硬配章位图。

## 4. 版式规范

- 基准：16 开 185×260mm；版心 135×216（天头22/地脚22/订口25/切口25）；正文 11pt/18pt 恋倍关系；页码地脚居中距裁口 ≥8mm。详见 `01-调研清单-图书与电子书制作规范.md`。
- **纸面必须纯白，禁止整页/版心铺底色**：`html,body{ background:#FFF }`。教训：整版心铺奶油底色（#F8F4E9）后，正文两端顶满色板边缘，色板外的白色页边距被读成"窗口背景"，用户连续两轮投诉"正文贴边"；像素测量证实页边距其实一直在（16–18mm），问题纯是底色破坏了页边的知觉归属。底色只允许用在卡片组件（提要/术语卡/按语）上作点缀。
- **注释/评注块的齐平式样（用户定稿）**：注文块与正文**左右同宽齐平**（不加 border-left、不做内缩阶梯），仅靠「徐注」描边章标签（加大加粗 9.2pt/700）+ 小一号灰棕注文（正文 82% 字号）区分层次。用户明确否决了"左竖线+左内缩"的签条式注文。
- **旧样章碎片段并入全书时必须接管样式**：遗留 `<section class="page">` 片段在新样式表中往往整组类名无定义（.rh/.para/.seal-tag/h3.sec…），会以浏览器默认样式混入；且缺 `break-before:page` 导致章首拼进上一区块页中、小标题孤悬地脚。并入时逐类补齐样式 + `.page{ break-before:page }` + `.para{ break-inside:avoid }`，标题链（h3.sec → .sec-sub → 首段）用 `break-after:avoid` 串住。
- **对读面板（签条式）**——结构陷阱与正解：
  ```html
  <div class="rcell">              <!-- 网格单元：position:relative，不放 overflow -->
    <span class="tag">王弼本</span> <!-- 签条：top:-5mm 凸出面板上边框 -->
    <div class="reader">           <!-- position:absolute; inset:0; overflow:hidden -->
      <div class="vtxt">…</div>    <!-- writing-mode:vertical-rl; height:100%; overflow:hidden -->
    </div>
  </div>
  ```
  陷阱说明：① 签条放面板内会压住竖排文字起头（间隙<1mm）；② 面板自身的 `overflow:hidden` 同时承担"锁定网格行高"职责（grid item 的 min-height:auto 会随内容撑大行），去掉它行高就会跑；但它又会裁掉凸出签条——所以必须"外层放签条、内层裁内容"分层。③ 竖排 vtxt 上加 padding-top 会让 Chromium 竖排折行失效（列溢出面板底部），禁止。
- **对读面板必须放在固定 `.page` 分区内，禁止处于流式上下文**：流式中的网格会被 Chromium 碎片化——竖排列失去右起锚定（贴靠左缘、右侧大片空置）、签条与空框残条跨页孤悬。全书生成时每章应拆为：章首固定页 → 对读上固定页 → 对读下+图例固定页 → 古注/今译/校勘流式（`break-before:page`）。
- **固定页与流式页混排必须用命名页解耦边距**：全书生成器若在固定页版式之后再声明 `@page{margin:20mm…}`（为流式章节设边距），会覆盖固定页的 `@page{margin:0}`——185mm 固定页被 Chromium 整体缩放进 145mm 版心（约 78% 缩放，封面/收束页四周出现白框，全书页数虚减、字号缩小，且极难察觉）。正解：`@page full{ size:…; margin:0 }` + `.page{ page:full }`，流式页走默认 `@page` 边距；命名页切换本身强制分页，与既有 break 规则恰好重合，不产生空白页。
- **流式内容之后的固定页必须 `break-before:page`**：固定页自身只有 `page-break-after`，前面的流式区块（古注/今译）排完后，下一个固定页会从当页半空续排（章首与上一章拼接、被跨页截断、只剩章旨框孤悬）。用 `body > .flow-sec ~ .page{ break-before:page }` 精准命中（避免封面在文档起点产生空白页）。相邻的 break-after:page + break-before:page 按规范合并为一个断点，不会产生空白页。
- 竖排文字：`writing-mode:vertical-rl` + `text-align:justify`；朱丝栏用 `repeating-linear-gradient(to left,…)` 画在文字层背景上，间距=行高。
- 章首：竖排章题（楷体）+ 配图占右 86mm + 导读 + 章旨框（左 1pt 朱线）。
- 对页版式：图/文字框成对时高度用固定值对齐，禁止 flex:1 自适应（基线不齐根源）。

## 5. 输出与 QA

- **PDF**：`build_pdf.sh`（渲染+字体审计+重渲）。出片后跑：
  - 字体审计（§1.3，必过）
  - `pdf_qa.py`（pdf skill 附带：元数据/空白页/溢出/边距）
  - judge 视觉验收：渲染 110dpi PNG 逐页送审；**judge 在 110dpi 下会误判字形细节（如 曰→日、简繁误读），凡涉及字形判读的 fail，先用 220dpi 高清裁切人工复核再改**（已两次误报）。
- **EPUB**：重排版（reflowable），CSS 全相对单位、图片 `max-width:100%`、脚注 `epub:type="footnote/noteref"`；`epubcheck` 必须零错误。
- **版式本（FXL，和 PDF 一致）**：重排版在任何阅读器都无法保证与 PDF 同观感（阅读器重排会吃掉版式）；要"和 PDF 一致"须用 EPUB3 Fixed Layout（`rendition:layout=pre-paginated` + 每页 viewport）。做法（`build_qtb_fxl.py`）：每页拆两层——① `add_redact_annot` 抹掉全部文字后渲染的图形层作背景图（卡片/饰线/书影/封面天然保留）；② 文字 span 逐个按 origin/bbox 绝对定位重建（真实文本可选中检索）。注意：Chromium 出片的 PDF 字体是 Type3，PyMuPDF 取不到 font/color 字段——字色用渲染像素反采样+设计色板吸附，字重按字号角色推断，逐字 span（letter-spacing 文本会被拆成单字）无需算字距。字体用 fontTools 按全书用字子集化嵌入（brotli 缺失时先 `pip install brotli`）。
- **验收循环**：render → judge → fix → 重渲受影响页 → 复审。每轮只重生成受影响页预览，但注意**预览图过期会造成 judge 误报**（改完必须刷新对应 PNG，且确认 judge 读的是新文件）。

## 6. 已知环境坑

| 坑 | 解法 |
|---|---|
| WebSearch/WebFetch 工具超时 | 用 `curl` 直接抓（本环境 curl 可用）；识典古籍/国图/luckclub 均为 SPA+反爬，取不到正文 |
| Edge 无头实例互锁卡死 | `pkill -9 -f "Microsoft Edge"` 后重跑；避免混用 --user-data-dir |
| 下载大字体文件中断 | curl 加 `-C - --retry 3` 续传；或换 jsDelivr/gstatic 子集 |
| github raw 大文件截断 | 校验文件头与声明长度（§1.2） |
| 帛书图版页序与行号不线性 | 以页边栏名定文献边界，勿跨篇混用行号 |
| 电子录文常带增补 B/C/F 区异体字（𤣥𠉀𫝊…），Noto 全字库不含增补区 | 装配时全库扫描 `ord(c)>0xFFFF`，按上下文逐字映射为通行正字（约 45–65 组）；否则印刷 PDF 缺字、FXL 子集字体亦无字形 |
| PyMuPDF 对 Type3 无 Unicode 映射的字形返回 `\x00` 等控制字符 | 生成 XML/EPUB 前必须剥除（esc 内 `re.sub(r'[\x00-\x08\x0b\x0c\x0e-\x1f\x7f-\x9f]','')`），否则 EPUB 解包校验报 not well-formed |
| 竖排面板文字截断无法靠 PDF 文本比对发现（竖排提取乱序） | 用字符计数法：按章比对面板页提取的汉字总数与渲染文本基线的差值（容差 4 字） |

## 7. 资产索引（仓库即打包）

```
仓库根/
├── SKILL.md                  ← 本文件（方法与规范）
├── scripts/                  ← 通用工具：fetch_fonts.sh（字体下载）/ render_audit.sh（渲染+审计）
│                              / stamp_folios.py（盖页码）
├── 01-调研清单…md            ← 开本/版心/字号/图文/注解/分页 全参数清单
├── 02-范例…md                ← 规格书范例
└── examples/daodejing/       ← 参考实现（原 道德经样张/，可整目录复制改名复用）
    ├── book.html             # 唯一内容与设计源
    ├── build_book_full.py    # 数据 → 全书 HTML + EPUB（含 clean_pairs/hsg_jing_safe 数据清洗）
    ├── content_81.py         # 各章章旨/今译（人工撰写层）
    ├── build_pdf.sh          # 渲染+审计+重渲
    ├── build_epub_sample.py  # 第一章样章 EPUB
    └── assets/               # 书影（公有领域古籍图版）
字体不入库：bash scripts/fetch_fonts.sh 下载（思源宋体全字库 OTF + 霞鹜文楷，断点续传+校验）。
```

## 8. 扩章流程（第一章 → 全书）

1. `work/book_data.json` 已含 81 章王弼/河上公/帛甲/帛乙文本与古注 → 逐章生成 section 追加进 book.html（模板复制第 5–11 页结构，替换文本与签条配色不变）。
2. 每完成一章：定位/核验该书影 → assets 追加 → 图题编号续排。
3. 全书合订后重跑 build_pdf.sh + 全量 judge + epubcheck；目录页与正文页码一致性最后核。
